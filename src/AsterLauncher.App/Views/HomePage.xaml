<Page
    x:Class="AsterLauncher.App.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto" VerticalScrollMode="Enabled">
        <Grid Padding="28" MaxWidth="1240" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock Text="首页" FontSize="34" FontWeight="SemiBold" Margin="0,4,0,4" Foreground="{ThemeResource PageTitleBrush}" />
            </StackPanel>

            <Border Grid.Row="1" CornerRadius="20" Margin="0,24,0,18" Padding="30" MinHeight="310">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#593A9F" Offset="0" />
                        <GradientStop Color="#23385B" Offset="0.52" />
                        <GradientStop Color="#126D71" Offset="1" />
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel MaxWidth="720" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Border Background="#44FFFFFF" CornerRadius="12" Padding="10,5">
                                <TextBlock Text="{Binding CurrentGame.InstallStatus}" FontSize="12" Foreground="White" />
                            </Border>
                            <Border Background="#24FFFFFF" CornerRadius="12" Padding="10,5">
                                <TextBlock Text="{Binding RunningStatusText}" FontSize="12" Foreground="White" />
                            </Border>
                        </StackPanel>
                        <TextBlock Text="{Binding CurrentGame.DisplayName}" FontSize="38" FontWeight="SemiBold" Margin="0,18,0,5" TextWrapping="Wrap" Foreground="White" />
                        <TextBlock Text="{Binding CurrentGame.Publisher}" Opacity="0.76" Foreground="White" />
                        <TextBlock Text="{Binding CurrentGame.ExecutablePath}" TextTrimming="CharacterEllipsis" Margin="0,24,0,0" Opacity="0.86" Foreground="White" />
                        <TextBlock Text="{Binding CompanionSummary}" TextTrimming="CharacterEllipsis" Margin="0,7,0,0" Opacity="0.7" Foreground="White" />
                    </StackPanel>
                    <TextBlock Grid.Column="1" Text="{Binding CurrentGame.IconGlyph}" FontSize="124" Opacity="0.24" VerticalAlignment="Center" Margin="40,0,12,0" Foreground="White" />
                </Grid>
            </Border>

            <Grid Grid.Row="2" ColumnSpacing="18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" MinWidth="300" />
                </Grid.ColumnDefinitions>
                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="15">
                        <TextBlock Text="开始游戏" Style="{StaticResource SectionTitleStyle}" />
                        <ComboBox
                            Header="启动方案"
                            ItemsSource="{Binding CurrentProfiles}"
                            SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                            DisplayMemberPath="Name"
                            HorizontalAlignment="Stretch" />
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Button Style="{StaticResource LaunchButtonStyle}" Content="{Binding LaunchButtonText}" IsEnabled="{Binding CanLaunch}" Click="LaunchButton_OnClick" />
                            <Button Content="重新扫描" Click="ScanButton_OnClick" />
                            <Button Content="手动指定" Click="ManualPathButton_OnClick" />
                            <ProgressRing Width="28" Height="28" IsActive="{Binding IsLaunching}" />
                        </StackPanel>
                        <InfoBar IsOpen="True" IsClosable="False" Severity="Informational" Title="当前状态" Message="{Binding StatusText}" />
                        <TextBlock Text="{Binding ScanStatus}" Style="{StaticResource MutedTextStyle}" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="14">
                        <TextBlock Text="会话摘要" Style="{StaticResource SectionTitleStyle}" />
                        <TextBlock Text="最近游玩" Style="{StaticResource MutedTextStyle}" />
                        <TextBlock Text="{Binding CurrentGame.LastPlayedText}" FontSize="16" />
                        <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />
                        <TextBlock Text="累计时长" Style="{StaticResource MutedTextStyle}" />
                        <TextBlock Text="{Binding CurrentGame.TotalPlayTimeText}" FontSize="16" />
                        <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />
                        <TextBlock Text="进程识别" Style="{StaticResource MutedTextStyle}" />
                        <TextBlock Text="适配器进程状态" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
